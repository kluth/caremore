/// <reference types="node" />
import { ServerResponse as S } from 'http';
export declare type ReadStreamOptions = Partial<{
    flags: string;
    encoding: BufferEncoding;
    fd: number;
    mode: number;
    autoClose: boolean;
    emitClose: boolean;
    start: number;
    end: number;
    highWaterMark: number;
}>;
export declare type SendFileOptions = ReadStreamOptions & Partial<{
    root: string;
    headers: Record<string, any>;
}>;
declare type Res = Pick<S, 'setHeader'> & NodeJS.WritableStream;
/**
 * Sends a file by piping a stream to response.
 *
 * It also checks for extension to set a proper `Content-Type` header.
 *
 * Path argument must be absolute. To use a relative path, specify the `root` option first.
 *
 * @param res Response
 */
export declare const sendFile: <Response_1 extends Res = Res>(res: Response_1) => (path: string, opts?: SendFileOptions, cb?: (err?: any) => void) => Response_1;
export {};
