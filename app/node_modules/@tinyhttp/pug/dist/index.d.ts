import { Options } from 'pug';
import type { App } from '@tinyhttp/app';
export declare const pug: (options?: Options) => (app: App<Options>) => App<Options, import("@tinyhttp/app").Request, import("@tinyhttp/app").Response<Options>>;
