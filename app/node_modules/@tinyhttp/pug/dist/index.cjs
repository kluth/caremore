'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var pug$1 = require('pug');

const pug = (options) => {
    return function pug(app) {
        return app.engine('pug', (path, _, opts, cb) => pug$1.renderFile(path, options || opts, cb));
    };
};

exports.pug = pug;
